name: Update SekaiDrama Cache

on:
  schedule:
    - cron: '0 */6 * * *' # Setiap 6 jam
  workflow_dispatch: # Memungkinkan jalankan manual dari tab Actions GitHub

permissions:
  contents: write

jobs:
  update-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run scrape script
        run: python scrape_nax.py

      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add database/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update SekaiDrama cache [skip ci]"
            git push
          fi

      - name: Purge jsDelivr CDN Cache for Database Pages
        run: |
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/trending.json
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/latest.json
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/populersearch.json
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/dubindo_terpopuler.json
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/dubindo_terbaru.json
          curl -s https://purge.jsdelivr.net/gh/xr3ed/M3U-Playlist-Player-Repo-for-Cloudstream@main/database/dramabox/foryou_1.json
